import 'package:flutter/material.dart';

final primaryColor = Colors.purple;
final secondaryHeaderColor = Colors.blue;
final Color primaryBackground = primaryColor;
final Color flightColor = primaryColor;
final Color chipBackground = secondaryHeaderColor.withOpacity(.2);
final Color borderColor = primaryColor.withAlpha(100);
